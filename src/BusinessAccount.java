public class BusinessAccount extends Account{
    public BusinessAccount(String name, int sortCode, int accountNumber, double balance) {
        super(name, sortCode, accountNumber, balance);
    }
}
